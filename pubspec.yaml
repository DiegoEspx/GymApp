name: proyectproducts
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  get: ^4.6.6
  get_storage: ^2.0.3
  cupertino_icons: ^1.0.8
  firebase_core: ^3.8.0 # Actualizado
  cloud_firestore: ^5.5.0 # Actualizado
  table_calendar: ^3.0.7 # Última versión estable
  qr_code_scanner: ^1.0.0
  qr_flutter: ^4.0.0 # Compatible
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0 # Actualizado

flutter:
  uses-material-design: true
  assets:
    - assets/images/1.webp
    - assets/images/2.jpg
    - assets/images/3.jpeg
    - assets/images/4.jpeg
